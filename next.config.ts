
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'lh3.googleusercontent.com', "m.media-amazon.com"],
  }
  /* config options here */
};

export default nextConfig;
